<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://code.jquery.com/jquery-2.2.1.min.js"></script>
    <title>Document</title>
    <script>
        function posting(){
            let title = $('#inputTitle').val();
            let author = $('#inputAuthor').val();
            let data = {'title': title, 'author': author}
            console.log(title);
            console.log(author);
            $.ajax({
                url:'/board',
                type:'post',
                contentType:'application/json',
                data: JSON.stringify(data),
                success: function (response){
                    alert("작성이 완료 되었습니다!")
                }
            })
        }
        
    </script>
</head>
<body>
    <h1>목록 추가하기</h1>
    <hr/>
    <form>
        <table>
            <tr>
                <td><input type="text" id="inputTitle" placeholder="제목을 입력하세요."></td>
            </tr>
            <tr>
                <td><input type="text" id="inputAuthor" placeholder="작성자를 입력하세요."></td>
            </tr>
        </table>
    </form>

    <button onclick="posting()">전송하기</button>
    <button>전송하기2</button>
</body>
</html>